import { TaskTwoDirective } from './task-two.directive';

describe('TaskTwoDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskTwoDirective();
    expect(directive).toBeTruthy();
  });
});
