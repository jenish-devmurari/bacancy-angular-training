import { TaskThreeDirective } from './task-three.directive';

describe('TaskThreeDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskThreeDirective();
    expect(directive).toBeTruthy();
  });
});
